# Ap_Activity4
URL Processing in Java

We were asked to parse url that can read the contents of a url entered by the user 
and identify all the <img> and <a> tags in the response.

Given java class is parsing url. It is html document and parsing <img> and <a> tags
